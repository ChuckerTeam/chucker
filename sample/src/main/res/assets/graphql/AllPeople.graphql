query people($first: Int, $last: Int) {
    allPeople(first: $first, last: $last) {
        totalCount
        pageInfo {
            startCursor
            endCursor
            hasNextPage
            hasPreviousPage
        }
        people {
            id
            birthYear
            created
            name
        }
    }
}